# linux-file-organizer
Bash script designed to analyze files and directories using Linux commands and parameter handling.  The script uses ls as the information source and allows listing:  Specific file information Largest files in a directory Smallest files in a directory Executable files only Combined filters and parameter validation
